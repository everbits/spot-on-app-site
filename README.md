# SpotOn App Site

This repo contains a small static site for SpotOn app-store support and privacy links.

Pages:

- `index.html` redirects to the support page
- `support/index.html` is the support page
- `privacy/index.html` is the privacy policy
- `styles.css` contains the shared styling

Update the company details, support copy, or privacy text directly in the HTML files.
